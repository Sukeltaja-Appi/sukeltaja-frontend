import React from 'react'
import { View } from 'react-native'

import styles from '../../../styles/global'

const SettingsScreen = () => <View style={styles.noPadding} />

export default SettingsScreen
